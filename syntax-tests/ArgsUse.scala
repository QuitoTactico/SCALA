// no sirve esta basura, ese tutorial qué, estará en otra versión??

object ArgsUse extends App{
    if (args.size == 0)
        println("Ola, miau, no hay argumentos pa")
    else
        println("Top argumentos: " + args(0))
}